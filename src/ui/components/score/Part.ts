import layout from "../../../layout";
import { Application } from "../../state/Application";

export const Part = (_application: Application, _context: CanvasRenderingContext2D, _part: layout.Part) => {
  // Nothing to do right now!
};
